

// Initiate counter
let counter = 0;

// Function to increment counter
function add() {
    let counter = 0;
    counter += 1;
}

// Call add() 3 times
add();
console.log(counter); // 0
add();
console.log(counter); // 0
add();
console.log(counter); // 0

//The counter should now be 3. But it is 0
